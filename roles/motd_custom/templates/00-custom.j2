#!/bin/bash
# Generated by Ansible. Deployment date: {{ motd_deployed_date }}

line="####################################"
hn="$(hostnamectl --static 2>/dev/null || hostname)"

echo "$line"
inside=$(( $(printf "%s" "$line" | wc -c) - 2 ))
printf "# %-*s #\n" "$inside" "Welcome to the ${hn}"
printf "# %-*s #\n" "$inside" "Last updated: {{ motd_deployed_date }}"
echo "$line"
