#plugin: community.proxmox.proxmox
collections:
  - community.general


plugin: community.general.proxmox
url:       https://192.168.2.112:8006
user:      ansible@pam
password:  Kb0ygd100
validate_certs: false
want_facts:     true

filters:
  - "'ubuntu' in (proxmox_tags_parsed|list)"

want_proxmox_nodes_ansible_host: false
compose:
#  ansible_host: proxmox_ipconfig0.ip | default(proxmox_net0.ip) | ipaddr('address')

#compose:
  ansible_host: "'{{ proxmox_agent_interfaces[1]['ip-addresses'][0] | regex_replace('/.*','') }}'"
#
#  ansible_host: "{{ proxmox_agent_interfaces[1]['ip-addresses'][0]  | ansible.utils.ipaddr('address') }}"
#  ansible_host: proxmox_ipconfig0.ip | default(proxmox_net0.ip) | ipaddr('address')
#  ansible_host: "{{ proxmox_agent_interfaces[1]['ip-addresses'][0] 
#                    | split('/') 
#                    | first }}"
  ansible_user: bob
